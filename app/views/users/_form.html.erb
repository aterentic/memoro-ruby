<%= form_for(@user) do |f| %>
	<%= render 'error_messages' %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
	<div class="actions">
    <%= f.submit %>
  </div>
<% end %>
